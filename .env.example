# Google Gemini API（作圖用）— 複製本檔為 .env 後填入真實值
# 實際 key 由 ~/hermes-slides/.env 取得；.env 已被 .gitignore 排除，切勿入 git
GEMINI_API_KEY=your_gemini_api_key_here

# 影像模型（可省略，預設 gemini-3-pro-image-preview）
GEMINI_IMAGE_MODEL=gemini-3-pro-image-preview
